Table _AgenticConfig(Id INTEGER,
UserId VARCHAR(500),
ComponentType NVARCHAR(200),
FrontEndJson NVARCHAR,
BackendJson NVARCHAR,
CreatedOn DATETIME,
ModifiedOn DATETIME)
Primary Key: _AgenticConfig(Id)

Table AgentModelMappings(ID INTEGER,
RefAgentID INTEGER,
RefModelID INTEGER)
Primary Key: AgentModelMappings(ID)

Table Agents(ID INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME,
RefUpdatedByID INTEGER,
UpdatedOn DATETIME,
Active BIT,
Name VARCHAR(500),
Description VARCHAR(1000),
SystemMessage VARCHAR,
ReflectOnToolUse BIT,
RefAgentTypeID INTEGER,
RefModelID INTEGER,
ToolCallSummaryFormat VARCHAR(500))
Primary Key: Agents(ID)

Table AgentSteps(ID INTEGER,
RefConversationID INTEGER,
RefConversationDetailsID INTEGER,
APICallCount INTEGER,
RequestMsg NVARCHAR,
APIResult NVARCHAR,
CreatedOn DATETIME2)
Primary Key: AgentSteps(ID)

Table AgentToolMapping(ID INTEGER,
RefAgentID INTEGER,
RefToolID INTEGER)
Primary Key: AgentToolMapping(ID)

Table AgentTypes(Id INTEGER,
Name VARCHAR(200),
Description VARCHAR,
SystemMessage NVARCHAR,
IsToolAssociated BIT,
IsToolAllowed BIT,
CreatedOn DATETIME,
IsActive BIT,
ModelClient VARCHAR(200))
Primary Key: AgentTypes(Id)

Table APIEndpoints(ID INTEGER,
DisplayName VARCHAR(150),
API VARCHAR(250),
Active BIT,
CreatedBy INTEGER,
CreatedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)

Table APIRequest_Response_Log(ID INTEGER,
RefConversationID INTEGER,
RefConversationDetailsID INTEGER,
APICallCount INTEGER,
RequestMsg NVARCHAR,
APIResult NVARCHAR,
ChatSessionID NVARCHAR(100),
CreatedOn DATETIME2)
Primary Key: APIRequest_Response_Log(ID)

Table Auth_RefreshTokens(Id BIGINT,
UserId BIGINT,
TokenHash NVARCHAR(256),
ExpiresAtUtc DATETIME2,
CreatedAtUtc DATETIME2,
RevokedAtUtc DATETIME2,
ReplacedByTokenHash NVARCHAR(256),
CreatedByIp NVARCHAR(64),
RevokedByIp NVARCHAR(64))
Primary Key: Auth_RefreshTokens(Id)

Table ChatDetails(ID INTEGER,
RefChatID INTEGER,
Request NVARCHAR,
RefRequestedByID INTEGER,
RequestedOn DATETIME,
Response NVARCHAR,
ResponseOn DATETIME,
InputTokens VARCHAR(50),
OutputTokens VARCHAR(50),
TotalTokens VARCHAR(50),
AIPlatform VARCHAR(50),
AIModel VARCHAR(50),
IsLike BIT,
IsDislike BIT,
DislikeTags VARCHAR(500),
DislikeFeedbackMessage NVARCHAR(500))
Primary Key: ChatDetails(ID)

Table Chats(ID INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME,
Active BIT,
Title NVARCHAR(500))
Primary Key: Chats(ID)

Table ClientChatbotConfiguration(Id INTEGER,
ClientName VARCHAR(200),
ClientID VARCHAR(500),
TokenID VARCHAR(500),
SessionID VARCHAR,
RefTeamID VARCHAR(200),
CreatedOn DATETIME,
Active BIT,
DefaultEmail VARCHAR(500))
Primary Key: ClientChatbotConfiguration(Id)

Table Clients(ID INTEGER,
Name VARCHAR(100),
ClientID VARCHAR(50),
ClientCertificate VARCHAR(200),
Active BIT)

Table CloudDeploymentDetails(Id INTEGER,
DeploymentJobID VARCHAR(200),
DeployedBy INTEGER,
DeployedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME,
RefPublishPodID INTEGER,
ComponentType VARCHAR(200),
RefCloudNameId INTEGER,
MethodType VARCHAR(200),
WorkflowJSON VARCHAR,
Status VARCHAR(200),
AuthHeader VARCHAR,
endpoint_ip VARCHAR(200),
endpoint_port VARCHAR(200),
instance_id VARCHAR(500),
protocol VARCHAR(200),
api_endpoint VARCHAR(200),
Active BIT,
Terminated BIT)
Primary Key: CloudDeploymentDetails(Id)

Table CloudName(Id INTEGER,
Name VARCHAR(200))
Primary Key: CloudName(Id)

Table Content_Template(ID INTEGER,
Title VARCHAR(100),
TemplateType VARCHAR(15),
GenAIPrompt NVARCHAR,
GenAIPromptforSection NVARCHAR,
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Content_Template(ID)

Table ConversationDetails(ID INTEGER,
ChatSessionID VARCHAR(200),
Request VARCHAR,
RefRequestedBy INTEGER,
RequestedOn DATETIME,
Response VARCHAR,
ResponseOn DATETIME,
AgenticFlow VARCHAR,
RefConversationID INTEGER,
isPromptFileUploaded BIT,
fileContent VARCHAR,
Active BIT,
IsLike BIT,
IsDislike BIT,
DislikeTags VARCHAR(500),
DislikeFeedbackMessage VARCHAR,
IsFeedbackMailSend BIT)
Primary Key: ConversationDetails(ID)

Table Conversations(ID INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME,
Active BIT,
ChatSessionID NVARCHAR(100),
RefTeamID INTEGER,
RefAgentID INTEGER,
JsonPayload NVARCHAR)
Primary Key: Conversations(ID)

Table Employees(Id INTEGER,
EmployeeCode INTEGER,
PersonId BIGINT,
PersonNumber BIGINT,
Title VARCHAR(200),
DisplayName VARCHAR(200),
FirstName VARCHAR(200),
LastName VARCHAR(200),
OrderName VARCHAR(200),
ListName VARCHAR(200),
FullName VARCHAR(200),
WorkEmail VARCHAR(200),
ShiftStartTime VARCHAR(200),
ShiftEndTime VARCHAR(200),
PersonalEmail VARCHAR(200),
MobileNumber VARCHAR(200),
DateOfBirth VARCHAR(200),
CreatedBy VARCHAR(200),
CreationDate VARCHAR(200),
PersonNameId VARCHAR(200),
DateOfJoining VARCHAR(200),
EffectiveEndDate VARCHAR(200),
LegalEmployerName VARCHAR(200),
WorkerType VARCHAR(200),
PrimaryFlag VARCHAR(200),
StartDate VARCHAR(200),
TerminationDate VARCHAR(200),
NotificationDate VARCHAR(200),
LastWorkingDate VARCHAR(200),
InsertedDateTime DATETIME,
ModifiedDateTime DATETIME)
Primary Key: Employees(Id)

Table ErrorLog(ErrorID INTEGER,
FunctionName NVARCHAR(200),
ErrorMessage NVARCHAR,
StackTrace NVARCHAR,
Parameters NVARCHAR,
LogTime DATETIME,
RefWorkflowID INTEGER,
RefCreatedBy INTEGER)
Primary Key: ErrorLog(ErrorID)

Table GoogleTokens(ID INTEGER,
UserId INTEGER,
AccessToken NVARCHAR,
RefreshToken NVARCHAR,
IssuedUtc DATETIME,
ExpiresInSeconds INTEGER)

Table LeadProcessing(ID INTEGER,
CreatedOn DATETIME,
UpdatedOn DATETIME,
RefLeadID INTEGER,
LeadType VARCHAR(500),
TeamID INTEGER,
Prompt VARCHAR,
Result VARCHAR,
Status VARCHAR(250))
Primary Key: LeadProcessing(ID)
Foreign Key: LeadProcessing(RefLeadID) references Leads(ID)

Table LeadPrompts(ID INTEGER,
CreatedOn DATETIME,
LeadType VARCHAR(500),
Prompt VARCHAR)
Primary Key: LeadPrompts(ID)

Table Leads(ID INTEGER,
CreatedOn DATETIME,
UpdatedOn DATETIME,
SenderName VARCHAR(255),
SenderEmail VARCHAR(255),
LeadStatus VARCHAR,
EmailSubject VARCHAR(500),
EmailBody VARCHAR,
AreaOfInterest VARCHAR(500),
LeadReceivedOn DATETIME,
Source VARCHAR(100),
SourceMessageID VARCHAR(500),
LeadPoint INTEGER,
SourceDomain VARCHAR(50),
Classification VARCHAR(100),
Designation VARCHAR(200),
call_log BIT,
MobileNo VARCHAR(200))
Primary Key: Leads(ID)

Table LeadScoreDetail(ID INTEGER,
Createdon DATETIME,
Updatedon DATETIME,
RefLeadScoreID INTEGER,
Type VARCHAR(100),
Link NVARCHAR(200),
Score INTEGER)
Primary Key: LeadScoreDetail(ID)
Foreign Key: LeadScoreDetail(RefLeadScoreID) references LeadScoring(ID)

Table LeadScoring(ID INTEGER,
CreatedOn DATETIME,
UpdatedOn DATETIME,
RefLeadID INTEGER,
Score INTEGER,
SessionId VARCHAR(50))
Primary Key: LeadScoring(ID)
Foreign Key: LeadScoring(RefLeadID) references Leads(ID)

Table ManualQueueDetails(ID INTEGER,
RefWorkflowsID INTEGER,
RefCreatedByID INTEGER,
RefUpdatedByID INTEGER,
RequestInput NVARCHAR,
Response NVARCHAR,
Status VARCHAR(20),
StartTime DATETIME,
EndTime DATETIME,
IsActive BIT,
CreatedOn DATETIME,
ModifiedOn DATETIME,
Output NVARCHAR)
Primary Key: ManualQueueDetails(ID)
Foreign Key: ManualQueueDetails(RefUpdatedByID) references Users(ID)

Table ModelKeyDetails(Id INTEGER,
RefModelId INTEGER,
CreatedBy INTEGER,
CreatedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME,
RefRoleId INTEGER,
SecretKey NVARCHAR(255),
Domain NVARCHAR(255),
IsDefault BIT,
IsActive BIT,
Description NVARCHAR)
Primary Key: ModelKeyDetails(Id)
Foreign Key: ModelKeyDetails(RefModelId) references Models(Id), ModelKeyDetails(CreatedBy) references Users(ID)

Table Models(Id INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME,
RefUpdatedByID INTEGER,
UpdatedOn DATETIME,
Active BIT,
Name NVARCHAR(500),
Description NVARCHAR,
Company NVARCHAR(500),
Context_Length INTEGER,
Type VARCHAR(500),
APIKey VARCHAR(500),
BaseURL VARCHAR(200),
Timeout VARCHAR(200),
MaxRetries INTEGER,
ModelParameters(JSON) VARCHAR,
RefProviderId INTEGER)
Primary Key: Models(Id)
Foreign Key: Models(RefProviderId) references Providers(Id)

Table ModuleFunctions(ID INTEGER,
RefModuleID INTEGER,
Name VARCHAR(50),
TourDescription VARCHAR(1000),
RefCreatedByID INTEGER,
CreatedOn DATETIME,
Active BIT)
Primary Key: ModuleFunctions(ID)
Foreign Key: ModuleFunctions(RefModuleID) references Modules(ID)

Table Modules(ID INTEGER,
RefParentModuleID INTEGER,
Name NVARCHAR(200),
Icon_class VARCHAR(50),
ControllerName VARCHAR(50),
ActionName VARCHAR(50),
TourDescription VARCHAR(1000),
RefCreatedBy INTEGER,
CreatedOn DATETIME,
Active BIT)
Primary Key: Modules(ID)

Table Notifications(ID INTEGER,
RefUserID INTEGER,
RefID INTEGER,
RefIDtype NVARCHAR(50),
NotificationType NVARCHAR(50),
AlertMessage NVARCHAR(500),
IsRead BIT,
CreatedOn DATETIME,
UpdatedOn DATETIME)
Primary Key: Notifications(ID)
Foreign Key: Notifications(RefUserID) references Users(ID)

Table NPIProduct(ID INTEGER,
ProductID NVARCHAR(200),
InputJson NVARCHAR,
OutputResult NVARCHAR,
CreatedOn DATETIME,
CreatedBy NVARCHAR(100),
UpdatedOn DATETIME,
Status NVARCHAR(100))
Primary Key: NPIProduct(ID)

Table Ownerships(ID INTEGER,
OwnershipTable VARCHAR(50),
RefID INTEGER,
RefUserID_SharedBy INTEGER,
RefUserID_SharedTo INTEGER,
RefRoleID_SharedTo INTEGER,
Access VARCHAR(50),
Comment NVARCHAR(500),
SharedDate DATETIME,
Permission VARCHAR(50))
Primary Key: Ownerships(ID)

Table PodCategory(ID INTEGER,
Category VARCHAR(500),
Active BIT,
CreatedBy INTEGER,
CreatedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: PodCategory(ID)

Table PodConfigurationLogs(ID INTEGER,
RefTeamID INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME,
PayloadJsonConfig NVARCHAR,
Active BIT)
Primary Key: PodConfigurationLogs(ID)

Table Providers(Id INTEGER,
ProviderName NVARCHAR)
Primary Key: Providers(Id)

Table PublishPods(ID INTEGER,
RefPodId INTEGER,
PodName VARCHAR(500),
Description VARCHAR,
Version DECIMAL(4, 2),
PublishBy INTEGER,
JsonConversation VARCHAR,
JsonVersion VARCHAR,
PublishOn DATETIME,
DescriptionText VARCHAR,
SamplePrompt VARCHAR,
UserRating INTEGER,
UserRatingCount INTEGER,
VideoPath VARCHAR(500))
Primary Key: PublishPods(ID)

Table PublishPodUserMapping(ID INTEGER,
RefPublishPodId INTEGER,
RefUserId INTEGER,
Active BIT)
Primary Key: PublishPodUserMapping(ID)
Foreign Key: PublishPodUserMapping(RefPublishPodId) references PublishPods(ID), PublishPodUserMapping(ID) references PublishPodUserMapping(ID)

Table PublishPodUserReViews(ID INTEGER,
RefPublishPodId INTEGER,
RefUserId INTEGER,
UserReviews NVARCHAR,
UserRating INTEGER,
Title VARCHAR(250),
CreatedOn DATETIME,
ModifiedOn DATETIME)
Primary Key: PublishPodUserReViews(ID)

Table RoleRestrictions(ID INTEGER,
RefRoleID INTEGER,
Name VARCHAR,
RestrictionBound BIT,
AllowedUploadcount INTEGER,
AllowedTransactioncount INTEGER,
Allowedtransactionperupload INTEGER)

Table Roles(ID INTEGER,
Name VARCHAR(50),
LandingPage VARCHAR(500),
RefModuleIds VARCHAR,
RefModuleFunctions VARCHAR,
IsAdmin BIT,
CreatedBy INTEGER,
CreatedOn DATETIME,
Active BIT)

Table ShareDriveApplications(ID INTEGER,
Name VARCHAR(200),
Active BIT,
CreatedOn DATETIME)
Primary Key: ShareDriveApplications(ID)

Table sysdiagrams(name NULL,
principal_id INTEGER,
diagram_id INTEGER,
version INTEGER,
definition VARBINARY)
Primary Key: sysdiagrams(diagram_id)

Table SystemVariables(ID INTEGER,
RefCreatedBy INTEGER,
CreatedOn DATETIME,
RefUpdatedBy INTEGER,
UpdatedOn DATETIME,
Name VARCHAR(1000),
Type VARCHAR(500),
Value NVARCHAR,
Active BIT)

Table TeamAgentMapping(ID INTEGER,
RefTeamID INTEGER,
RefAgentID INTEGER)
Primary Key: TeamAgentMapping(ID)

Table Teams(ID INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME,
RefUpdatedByID INTEGER,
UpdatedOn DATETIME,
Active BIT,
Name VARCHAR(200),
Description VARCHAR(1000),
RefTeamTypeID INTEGER,
RefCategoryID INTEGER,
SystemMessage VARCHAR,
Published BIT,
SendFeedbackMail BIT,
FeedbackEmailID NVARCHAR(250),
IsLikeDislike BIT,
DislikeTags NVARCHAR,
CCEmailID NVARCHAR(250))
Primary Key: Teams(ID)

Table TeamsModelMappings(ID INTEGER,
RefTeamID INTEGER,
RefModelID INTEGER)
Primary Key: TeamsModelMappings(ID)

Table TeamTypes(Id INTEGER,
Name VARCHAR(500),
Description NVARCHAR,
ModelClient VARCHAR(500),
SystemMessage VARCHAR(1000),
IsTerminationAllowed BIT,
TerminationText VARCHAR(500),
MaxMessageLimit INTEGER,
CreatedOn DATETIME,
IsActive BIT)
Primary Key: TeamTypes(Id)

Table Template(ID INTEGER,
Title VARCHAR(100),
TemplateType VARCHAR(15),
GenAIPrompt NVARCHAR,
GenAIPromptforSection NVARCHAR,
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Template(ID)

Table Tools(ID INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME,
RefUpdatedByID INTEGER,
UpdatedOn DATETIME,
Active BIT,
Name VARCHAR(200),
Description VARCHAR(1000),
FunctionName VARCHAR(200),
Language VARCHAR(200),
InputDescription VARCHAR(1000),
OutputDescription VARCHAR(1000),
References VARCHAR(1000),
Code VARCHAR(200),
Namespaces VARCHAR(200),
Functions VARCHAR(200))
Primary Key: Tools(ID)

Table TranslationPrompts(ID INTEGER,
Language NVARCHAR(50),
Prompt NVARCHAR,
Active BIT,
ReplacementWord VARCHAR)
Primary Key: TranslationPrompts(ID)

Table UserAccessTokens(ID INTEGER,
CreatedByID INTEGER,
UpdatedByID INTEGER,
CreatedOn DATETIME,
UpdatedOn DATETIME,
RefChatSessionID NVARCHAR(100),
AccessToken NVARCHAR,
Platform VARCHAR(200),
TokenExpireAt DATETIME)
Primary Key: UserAccessTokens(ID)

Table Users(ID INTEGER,
FirstName VARCHAR(50),
LastName VARCHAR(50),
Email VARCHAR(200),
Password VARCHAR(500),
ApiPassword VARCHAR(500),
AuthenticationType VARCHAR(50),
RefRoleID INTEGER,
LastLogin DATETIME,
LandingPage VARCHAR(500),
Active BIT,
VerificationCode VARCHAR(100),
CreatedOn DATETIME,
LoginAttemptFailed INTEGER,
AdditionalInfo VARCHAR,
ActivationMailSent BIT)
Primary Key: Users(ID)

Table VersionInfo(Version BIGINT,
AppliedOn DATETIME,
Description NVARCHAR(1024))

Table WorkFlowExecutionDetails(ID BIGINT,
RefWorkflowExecutionID UNIQUEIDENTIFIER,
StepDescription NVARCHAR,
InputValue NVARCHAR,
Output NVARCHAR,
InnerJobName VARCHAR(100),
NodeId VARCHAR(250),
StartedOn DATETIME,
CompletedOn DATETIME,
Status VARCHAR(100),
ErrorMessage VARCHAR)
Primary Key: WorkFlowExecutionDetails(ID)

Table WorkFlowExecutions(ID UNIQUEIDENTIFIER,
RefWorkFlowID VARCHAR(500),
ExecutedOn INTEGER,
ExecutedBy INTEGER,
StartedOn DATETIME,
CompletedOn DATETIME,
Status INTEGER,
ScheduleID INTEGER,
ErrorMessage VARCHAR,
WorkflowStatus VARCHAR(100),
WorkflowConfigData NVARCHAR)
Primary Key: WorkFlowExecutions(ID)

Table Workflows(ID INTEGER,
RefCreatedByID INTEGER,
CreatedOn DATETIME2,
RefUpdatedByID INTEGER,
ModifiedOn DATETIME2,
Name NVARCHAR(255),
Description NVARCHAR,
Data NVARCHAR,
IsAutoSave BIT,
Version NVARCHAR(20),
IsActive BIT,
UserId INTEGER,
Tags NVARCHAR(500),
RefClientId VARCHAR(50),
Ratings INTEGER)
Primary Key: Workflows(ID)

Table Workflows_DropboxConnection(ID INTEGER,
RefWorkflowID INTEGER,
ConnectionName VARCHAR(150),
ConnectionType VARCHAR(150),
CheckedItem VARCHAR(50),
AccessToken NVARCHAR,
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Workflows_DropboxConnection(ID)

Table Workflows_FTPConnection(ID INTEGER,
RefWorkflowID INTEGER,
ConnectionName VARCHAR(150),
FtpUrl VARCHAR(150),
FtpPort VARCHAR(15),
userid VARCHAR(150),
Password VARCHAR(150),
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Workflows_FTPConnection(ID)

Table Workflows_GmailConnection(ID INTEGER,
RefWorkflowID INTEGER,
ConnectionName VARCHAR(150),
ConnectionType VARCHAR(150),
CheckedItem VARCHAR(50),
AccessToken NVARCHAR,
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Workflows_GmailConnection(ID)

Table Workflows_GoogleDriveConnection(ID INTEGER,
RefWorkflowID INTEGER,
ConnectionName VARCHAR(150),
ConnectionType VARCHAR(150),
CheckedItem VARCHAR(50),
AccessToken NVARCHAR,
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Workflows_GoogleDriveConnection(ID)

Table Workflows_Nodes(ID INTEGER,
NodeName VARCHAR(150),
NodeType VARCHAR(50),
NodeIcon VARCHAR(50),
NodeColor VARCHAR(50),
IsActive BIT,
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Workflows_Nodes(ID)

Table Workflows_ShareDriveConnections(ID INTEGER,
RefWorkflowID INTEGER,
RefShareDriveApplicationID INTEGER,
ConnectionName VARCHAR(150),
ConnectionType VARCHAR(150),
CheckedItem VARCHAR(50),
AccessToken NVARCHAR,
AddedBy INTEGER,
AddedOn DATETIME,
UpdatedBy INTEGER,
UpdatedOn DATETIME)
Primary Key: Workflows_ShareDriveConnections(ID)

Table Workflows_WebhookAuthFields(ID INTEGER,
RefWorkflowId INTEGER,
RefUserId INTEGER,
WebhookUrl VARCHAR(550),
Type VARCHAR(50),
KeyName VARCHAR,
KeyValue VARCHAR)
Primary Key: Workflows_WebhookAuthFields(ID)

Table WorkFlowVariables(ID INTEGER,
RefWorkflowID INTEGER,
RefCreatedBy INTEGER,
CreatedOn DATETIME,
RefUpdatedBy INTEGER,
UpdatedOn DATETIME,
Name VARCHAR(1000),
Type VARCHAR(500),
Value NVARCHAR,
ValueType VARCHAR(300),
UsageSyntax VARCHAR(500),
Active BIT,
Secure BIT)
Primary Key: WorkFlowVariables(ID)
Foreign Key: WorkFlowVariables(RefWorkflowID) references Workflows(ID)